language: julia
os:
    - linux
julia:
    - release
    - nightly
notifications:
    email: false
after_success:
    - julia -e 'Pkg.add("Compat")'
    - julia -e 'cd(Pkg.dir("STCA"));Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
